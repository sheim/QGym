{
    "method": "grid",
    "metric": {
        "name": "rewards/total_rewards",
        "goal": "maximize"
    },
    "run_cap": 50,
    "parameters": {
        "train_cfg.critic.reward.weights.theta":{
            "values": [1.0, 10.0]
        },
        "train_cfg.critic.reward.weights.omega":{
            "values": [1.0, 10.0]
        },
        "train_cfg.critic.reward.weights.equilibrium":{
            "values": [1.0, 10.0]
        },
        "train_cfg.critic.reward.weights.energy":{
            "values": [1.0, 10.0]
        }
    }
}